import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // output: 'export',
  reactStrictMode: true,
  // basePath: '/nextroutes', // Укажите имя репозитория
  // images: {
  //   domains: ['localhost', 'flagcdn.com', 'upload.wikimedia.org'],
  //   unoptimized: true,
  // },


};

export default nextConfig;
